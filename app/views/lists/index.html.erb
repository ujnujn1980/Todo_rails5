<p id="notice"><%= notice %></p>

<h1>Lists</h1>

<table>
  <thead>
    <tr>
      <th>List id</th>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @lists.each do |list| %>
      <tr>
	      <!--    <td><%= list.name %></td> -->
    <!--    <td><%= link_to list.name, edit_list_path(list) %></td> -->
    <!--    <td><%= link_to list.name, item_path(list) %></td> -->
    <!--    <td><%= link_to list.name, items_path(list_id: list.id) %></td> -->
    <!--     <td><%= link_to list.name, items_path(list) %></td> -->
        <td><%= link_to list.id, items_path(list_id: list.id) %></td> 
        <td><%= link_to list.name, items_path(list_id: list.id) %></td> 
        <td><%= link_to 'Edit',    edit_list_path(list) %></td>
        <td><%= link_to 'Destroy', list, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New List', new_list_path %>
<%= link_to 'HOME', home_index_path %>
